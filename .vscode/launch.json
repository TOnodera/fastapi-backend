{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Python: Module",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "cwd": "${workspaceFolder}",
            "args": [
                "src.main:app",
                "--reload"
            ],
            "env": {
                "IS_TEST": "1" //テスト用DB使用
            }
        }
    ]
}